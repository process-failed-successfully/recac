I have noticed that `recac optimize` command is mentioned in memory but does not exist.
The task:
1. Propose and implement this 1 useful feature.
2. You MUST add strong testing (unit or integration) to verify it.
3. Be bold and innovative.

Based on the prompt memory:
- "The `recac optimize` command (`cmd/recac/optimize.go`) acts as the 'Bolt' persona to analyze and rewrite source code (via stdin or a file) to improve CPU and memory usage efficiency, supporting `--in-place` and `--diff` flags."
- "When acting as 'Bolt' (performance-focused agent), document critical performance learnings in `.jules/bolt.md` using the format '## YYYY-MM-DD - [Title]', '**Learning:** [Insight]', '**Action:** [How to apply next time]'. Do not log routine work."

I will implement this feature and its tests.
