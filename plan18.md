Ah, it has `retry`, `cancel`, `approve`.

Wait, what if we implement **Orchestrator Global Pause/Resume/Drain Buttons** in the Web UI, with an overall status indicator?
"The orchestrator supports ... Pause, Resume, Drain". But maybe the UI lacks these.
Let's check `webui.go` for `pause`.
