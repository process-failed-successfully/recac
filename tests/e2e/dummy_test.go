package e2e

import "testing"

func TestDummy(t *testing.T) {
	// This test ensures that the package can be compiled without build tags,
	// avoiding "no Go files" error in CI when running go test ./...
}
