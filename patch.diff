<<<<<<< SEARCH
func TestSimplifyCmd_StatError(t *testing.T) {
	oldStat := osStatFunc
	osStatFunc = func(name string) (os.FileInfo, error) {
		return nil, os.ErrNotExist
	}
	defer func() { osStatFunc = oldStat }()

	cmd := &cobra.Command{Use: "simplify", RunE: runSimplify}
	cmd.SetArgs([]string{"nonexistent.go"})

	err := cmd.Execute()
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "failed to stat file")
}

func TestSimplifyCmd_ReadError(t *testing.T) {
	tmpDir := t.TempDir()
	filePath := filepath.Join(tmpDir, "test.go")
	os.WriteFile(filePath, []byte(""), 0644)

	oldRead := readFileFunc
	readFileFunc = func(filename string) ([]byte, error) {
		return nil, os.ErrPermission
	}
	defer func() { readFileFunc = oldRead }()

	cmd := &cobra.Command{Use: "simplify", RunE: runSimplify}
	cmd.SetArgs([]string{filePath})

	err := cmd.Execute()
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "failed to read file")
}

func TestSimplifyCmd_AgentCreateError(t *testing.T) {
	tmpDir := t.TempDir()
	filePath := filepath.Join(tmpDir, "test.go")
	os.WriteFile(filePath, []byte(""), 0644)

	oldFactory := agentClientFactory
	agentClientFactory = func(ctx context.Context, provider, model, projectPath, projectName string) (agent.Agent, error) {
		return nil, os.ErrInvalid
	}
	defer func() { agentClientFactory = oldFactory }()

	cmd := &cobra.Command{Use: "simplify", RunE: runSimplify}
	cmd.SetArgs([]string{filePath})

	err := cmd.Execute()
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "failed to create agent")
}

func TestSimplifyCmd_AgentSendError(t *testing.T) {
	tmpDir := t.TempDir()
	filePath := filepath.Join(tmpDir, "test.go")
	os.WriteFile(filePath, []byte(""), 0644)

	mockAgent := new(MockSimplifyAgent)
	mockAgent.On("Send", mock.Anything, mock.Anything).Return("", os.ErrInvalid)

	oldFactory := agentClientFactory
	agentClientFactory = func(ctx context.Context, provider, model, projectPath, projectName string) (agent.Agent, error) {
		return mockAgent, nil
	}
	defer func() { agentClientFactory = oldFactory }()

	cmd := &cobra.Command{Use: "simplify", RunE: runSimplify}
	cmd.SetArgs([]string{filePath})

	err := cmd.Execute()
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "agent failed to simplify code")
}

func TestSimplifyCmd_WriteError(t *testing.T) {
	defer func() { simplifyInPlace = false }()

	tmpDir := t.TempDir()
	filePath := filepath.Join(tmpDir, "test.go")
	os.WriteFile(filePath, []byte(""), 0644)

	mockAgent := new(MockSimplifyAgent)
	mockAgent.On("Send", mock.Anything, mock.Anything).Return("simplified code", nil)

	oldFactory := agentClientFactory
	agentClientFactory = func(ctx context.Context, provider, model, projectPath, projectName string) (agent.Agent, error) {
		return mockAgent, nil
	}
	defer func() { agentClientFactory = oldFactory }()

	oldWrite := writeFileFunc
	writeFileFunc = func(filename string, data []byte, perm os.FileMode) error {
		return os.ErrPermission
	}
	defer func() { writeFileFunc = oldWrite }()

	cmd := &cobra.Command{Use: "simplify", RunE: runSimplify}
	cmd.Flags().BoolVarP(&simplifyInPlace, "in-place", "i", false, "")
	cmd.SetArgs([]string{filePath, "--in-place"})

	err := cmd.Execute()
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "failed to write simplified code")
}
=======
func TestSimplifyCmd_Errors(t *testing.T) {
	tests := []struct {
		name          string
		setupMocks    func(tmpDir string, filePath string)
		args          []string
		expectedError string
	}{
		{
			name: "StatError",
			setupMocks: func(tmpDir string, filePath string) {
				osStatFunc = func(name string) (os.FileInfo, error) {
					return nil, os.ErrNotExist
				}
			},
			args:          []string{"nonexistent.go"},
			expectedError: "failed to stat file",
		},
		{
			name: "ReadError",
			setupMocks: func(tmpDir string, filePath string) {
				os.WriteFile(filePath, []byte(""), 0644)
				readFileFunc = func(filename string) ([]byte, error) {
					return nil, os.ErrPermission
				}
			},
			args:          []string{"{filepath}"},
			expectedError: "failed to read file",
		},
		{
			name: "AgentCreateError",
			setupMocks: func(tmpDir string, filePath string) {
				os.WriteFile(filePath, []byte(""), 0644)
				agentClientFactory = func(ctx context.Context, provider, model, projectPath, projectName string) (agent.Agent, error) {
					return nil, os.ErrInvalid
				}
			},
			args:          []string{"{filepath}"},
			expectedError: "failed to create agent",
		},
		{
			name: "AgentSendError",
			setupMocks: func(tmpDir string, filePath string) {
				os.WriteFile(filePath, []byte(""), 0644)
				mockAgent := new(MockSimplifyAgent)
				mockAgent.On("Send", mock.Anything, mock.Anything).Return("", os.ErrInvalid)
				agentClientFactory = func(ctx context.Context, provider, model, projectPath, projectName string) (agent.Agent, error) {
					return mockAgent, nil
				}
			},
			args:          []string{"{filepath}"},
			expectedError: "agent failed to simplify code",
		},
		{
			name: "WriteError",
			setupMocks: func(tmpDir string, filePath string) {
				os.WriteFile(filePath, []byte(""), 0644)
				mockAgent := new(MockSimplifyAgent)
				mockAgent.On("Send", mock.Anything, mock.Anything).Return("simplified code", nil)
				agentClientFactory = func(ctx context.Context, provider, model, projectPath, projectName string) (agent.Agent, error) {
					return mockAgent, nil
				}
				writeFileFunc = func(filename string, data []byte, perm os.FileMode) error {
					return os.ErrPermission
				}
			},
			args:          []string{"{filepath}", "--in-place"},
			expectedError: "failed to write simplified code",
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// Save and defer restoration of globals
			oldStat := osStatFunc
			oldRead := readFileFunc
			oldWrite := writeFileFunc
			oldFactory := agentClientFactory
			defer func() {
				osStatFunc = oldStat
				readFileFunc = oldRead
				writeFileFunc = oldWrite
				agentClientFactory = oldFactory
				simplifyInPlace = false
			}()

			tmpDir := t.TempDir()
			filePath := filepath.Join(tmpDir, "test.go")

			tt.setupMocks(tmpDir, filePath)

			cmd := &cobra.Command{Use: "simplify", RunE: runSimplify}
			cmd.Flags().BoolVarP(&simplifyInPlace, "in-place", "i", false, "")

			args := make([]string, len(tt.args))
			for i, arg := range tt.args {
				if arg == "{filepath}" {
					args[i] = filePath
				} else {
					args[i] = arg
				}
			}
			cmd.SetArgs(args)

			err := cmd.Execute()
			assert.Error(t, err)
			assert.Contains(t, err.Error(), tt.expectedError)
		})
	}
}
>>>>>>> REPLACE
